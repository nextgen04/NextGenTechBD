# 🚀 NextGenTechBD — GitHub + Cloudflare Pages + D1 দিয়ে পাবলিশ করার গাইড

> **মোট সময়:** ৪৫–৬০ মিনিট | **খরচ:** ৳০ (Cloudflare-এর ফ্রি টিয়ারে যথেষ্ট)
> **যা লাগবে:** একটা ইমেইল, GitHub অ্যাকাউন্ট, Cloudflare অ্যাকাউন্ট, কম্পিউটারে [Node.js](https://nodejs.org) (LTS ভার্সন) ইনস্টল করা থাকতে হবে (শুধু ডেটাবেজ সেটআপের জন্য একবার লাগবে)

**এই প্রজেক্টে যা আছে:**
- `public/` — সাইটের ফ্রন্টএন্ড (আগের মতোই, শুধু এখন ডেটা আসে নিজের ব্যাকএন্ড API থেকে)
- `functions/` — Cloudflare Pages Functions দিয়ে বানানো ব্যাকএন্ড API (D1 ডেটাবেজের সাথে কথা বলে)
- `schema.sql` — ডেটাবেজের টেবিল কাঠামো
- `seed.sql` — আপনার আগের পণ্য/ক্যাটাগরি/সেটিংস ডেটা (প্রথমবার ডেটাবেজে ঢোকানোর জন্য)
- `public/admin/` — নতুন কাস্টম অ্যাডমিন প্যানেল (নিজের ব্যাকএন্ড API ব্যবহার করে, Netlify/Decap CMS আর লাগবে না)

**পাবলিশ হলে আপনি পাবেন:**
- 🌐 সাইটের লিংক (যেমন: `https://nextgentechbd.pages.dev`)
- 🗄️ নিজস্ব D1 (SQL) ডেটাবেজ — পণ্য, ক্যাটাগরি, সেটিংস, অর্ডার সব ডেটাবেজে জমা থাকবে
- 🔐 কাস্টম অ্যাডমিন প্যানেল — একটা সিক্রেট কী দিয়ে লগইন করে পণ্য/সেটিংস/অর্ডার ম্যানেজ করা যাবে
- 💬 প্রতিটি অর্ডার হোয়াটসঅ্যাপে কাস্টমারের কাছ থেকে আসবে + একই সাথে ডেটাবেজেও সেভ হবে

---

## 🐙 ধাপ ১: GitHub-এ কোড আপলোড (৭ মিনিট)

1. **github.com** খুলুন → লগইন করুন (না থাকলে **Sign up**)
2. ডান-উপরের **＋** → **New repository**
3. Repository name: `nextgentechbd` → **Public** অথবা **Private** (দুটোই কাজ করবে) → **Create repository**
4. পরের পেজে **"uploading an existing file"** লিংকে ক্লিক করুন
5. এই ফোল্ডারের সব ফাইল-ফোল্ডার (`public`, `functions`, `schema.sql`, `seed.sql`, `wrangler.toml`, `package.json` ইত্যাদি — **সব একসাথে**) সিলেক্ট করে ড্র্যাগ-অ্যান্ড-ড্রপ করুন
6. নিচে **Commit changes** চাপুন

✅ কোড এখন GitHub-এ সংরক্ষিত!

---

## ▲ ধাপ ২: Cloudflare Pages-এ সাইট ডেপ্লয় করুন (৮ মিনিট)

1. **dash.cloudflare.com** খুলুন → লগইন/সাইন আপ করুন (ফ্রি)
2. বাম মেনু থেকে **Workers & Pages** → **Create** → **Pages** ট্যাব → **Connect to Git**
3. GitHub অ্যাকাউন্ট কানেক্ট করুন (অনুমতি দিন) → আপনার **nextgentechbd** রিপোজিটরি বেছে নিন → **Begin setup**
4. বিল্ড সেটিংস দিন:

   | ঘর | মান |
   |---|---|
   | Project name | `nextgentechbd` (বা যা খুশি) |
   | Production branch | `main` |
   | Framework preset | **None** |
   | Build command | (খালি রাখুন) |
   | Build output directory | `public` |

5. **Save and Deploy** চাপুন → ১–২ মিনিট অপেক্ষা করুন
6. ডেপ্লয় শেষ হলে একটা লিংক পাবেন (যেমন `https://nextgentechbd.pages.dev`) — সাইট এখন লাইভ, কিন্তু এখনো ডেটাবেজ ছাড়া তাই পণ্য দেখাবে না। পরের ধাপে সেটা ঠিক করব।

---

## 🗄️ ধাপ ৩: D1 ডেটাবেজ তৈরি করুন (৫ মিনিট)

1. Cloudflare ড্যাশবোর্ডে বাম মেনু থেকে **Workers & Pages** → **D1 SQL Database** → **Create Database**
2. Database name দিন: `nextgentechbd-db` → **Create**
3. ডেটাবেজ তৈরি হলে উপরে একটা **Database ID** দেখাবে — এটা কপি করে রাখুন (পরে লাগবে)

---

## 🔗 ধাপ ৪: ডেটাবেজটি আপনার Pages প্রজেক্টের সাথে যুক্ত করুন (৩ মিনিট)

1. **Workers & Pages** → আপনার **nextgentechbd** (Pages) প্রজেক্টে ঢুকুন → **Settings** ট্যাব → **Functions** → **D1 database bindings**
2. **Add binding** চাপুন:
   - Variable name: **`DB`** (ঠিক এই নামেই দিতে হবে, বড়/ছোট হাতের অক্ষর মিলিয়ে — কোডে এই নামেই ব্যবহার করা হয়েছে)
   - D1 database: `nextgentechbd-db` বেছে নিন
3. **Save** করুন
4. `wrangler.toml` ফাইলে `database_id = "YOUR-DATABASE-ID-HERE"` জায়গায় ধাপ ৩-এ কপি করা Database ID বসিয়ে GitHub-এ ফাইলটা আপডেট করুন (ঐচ্ছিক — শুধু আপনি পরে লোকালি `wrangler` কমান্ড চালাতে চাইলে দরকার হবে)

---

## 🧱 ধাপ ৫: ডেটাবেজে টেবিল ও পুরনো পণ্যের ডেটা লোড করুন (১০ মিনিট)

এই ধাপে আপনার কম্পিউটারে **Node.js** ইনস্টল থাকা লাগবে ([nodejs.org](https://nodejs.org) থেকে LTS ভার্সন ইনস্টল করুন যদি না থাকে)।

1. GitHub থেকে রিপোজিটরিটা কম্পিউটারে ক্লোন করুন অথবা ডাউনলোড (Code → Download ZIP) করে আনজিপ করুন
2. টার্মিনাল/কমান্ড প্রম্পট খুলে সেই ফোল্ডারে যান, যেমন:
   ```bash
   cd nextgentechbd
   ```
3. Cloudflare-এ লগইন করুন:
   ```bash
   npx wrangler login
   ```
   (ব্রাউজার খুলবে, Cloudflare অ্যাকাউন্ট দিয়ে অনুমতি দিন)
4. টেবিল কাঠামো (schema) তৈরি করুন:
   ```bash
   npx wrangler d1 execute nextgentechbd-db --remote --file=./schema.sql
   ```
5. পুরনো পণ্য/ক্যাটাগরি/সেটিংস ডেটা লোড করুন:
   ```bash
   npx wrangler d1 execute nextgentechbd-db --remote --file=./seed.sql
   ```
6. দুটো কমান্ডই "🚣 Executed X queries" জাতীয় সবুজ মেসেজ দেখালে বুঝবেন সফল হয়েছে

> 💡 **বিকল্প (Node.js ছাড়া):** Cloudflare ড্যাশবোর্ড → D1 → `nextgentechbd-db` → **Console** ট্যাব থেকেও `schema.sql` এবং `seed.sql` ফাইলের ভেতরের SQL কপি-পেস্ট করে চালানো যায়, তবে wrangler CLI ব্যবহার করাই সবচেয়ে সহজ ও নির্ভরযোগ্য।

---

## 🔐 ধাপ ৬: অ্যাডমিন কী (ADMIN_KEY) সেট করুন (৩ মিনিট)

অ্যাডমিন প্যানেলে লগইন করতে একটা সিক্রেট কী লাগবে — এটাই এখন আপনার "পাসওয়ার্ড"।

1. প্রথমে একটা শক্তিশালী র‍্যান্ডম কী বানিয়ে নিন (কমপক্ষে ১৬-২০ ক্যারেক্টার, যেমন: `Ngtbd#2026$SecureKey9x`)
2. Cloudflare ড্যাশবোর্ডে আপনার **nextgentechbd** Pages প্রজেক্টে ঢুকুন → **Settings** → **Environment variables**
3. **Production** এর নিচে **Add variable** চাপুন:
   - Variable name: `ADMIN_KEY`
   - Value: আপনার বানানো সিক্রেট কী
   - **Encrypt** (🔒) অপশনটা চালু রাখুন যাতে এটা গোপন থাকে
4. **Save** করুন

⚠️ এই কী কারো সাথে শেয়ার করবেন না — যার কাছে এই কী থাকবে সে-ই পণ্য/সেটিংস বদলাতে পারবে।

---

## ♻️ ধাপ ৭: রিডেপ্লয় করুন (২ মিনিট)

Environment variable বা D1 বাইন্ডিং যোগ করার পর সাইট একবার রিডেপ্লয় করতে হবে যাতে পরিবর্তনগুলো কার্যকর হয়:

1. Pages প্রজেক্টে **Deployments** ট্যাবে যান
2. সর্বশেষ ডেপ্লয়মেন্টের পাশে **⋯** → **Retry deployment** চাপুন (অথবা GitHub-এ যেকোনো ছোট পরিবর্তন কমিট করলেও অটো রিডেপ্লয় হবে)
3. ১-২ মিনিট পর সাইটের লিংক খুলে দেখুন — এখন পণ্যগুলো দেখা উচিত ✅

---

## 🖥️ ধাপ ৮: অ্যাডমিন প্যানেল ব্যবহার করুন

1. ব্রাউজারে যান: `https://আপনার-সাইট.pages.dev/admin/`
2. ধাপ ৬-এ বানানো **ADMIN_KEY** দিয়ে **লগইন করুন**
3. চারটা ট্যাব দেখবেন:
   - **🛍️ পণ্যসমূহ** — পণ্য যোগ/এডিট/মুছা, দাম, ছবির ইমোজি, ফ্ল্যাশ সেল টগল ইত্যাদি
   - **🗂️ ক্যাটাগরি** — নতুন ক্যাটাগরি যোগ/এডিট/মুছা
   - **⚙️ দোকানের সেটিংস** — হোয়াটসঅ্যাপ/টেলিগ্রাম/ফেসবুক/ইমেইল ও বিকাশ/নগদ/রকেট নম্বর বদলানো
   - **📦 অর্ডারসমূহ** — সব অর্ডার দেখা ও স্ট্যাটাস (অপেক্ষমান/নিশ্চিত/পাঠানো হয়েছে/ডেলিভার্ড/বাতিল) বদলানো
4. Save করলেই সরাসরি ডেটাবেজে সংরক্ষণ হয় এবং সাথে সাথেই সাইটে (রিফ্রেশ করলে) দেখা যাবে — কোনো রিবিল্ড/অপেক্ষা লাগে না, কারণ ডেটা এখন সরাসরি API থেকে আসে

---

## 💬 ধাপ ৯: অর্ডার কীভাবে আসবে

- কাস্টমার চেকআউট করলে তার ব্রাউজারে **হোয়াটসঅ্যাপ চ্যাট** খুলে যাবে, অর্ডারের বিস্তারিত প্রি-ফিল করা থাকবে — কাস্টমার শুধু **Send** চাপবে
- একই সাথে অর্ডারের একটা কপি আপনার **D1 ডেটাবেজে** সেভ হবে, এবং একটা ইউনিক **অর্ডার আইডি** (যেমন `SB12345678`) তৈরি হবে
- অ্যাডমিন প্যানেলের **📦 অর্ডারসমূহ** ট্যাবে গিয়ে সব অর্ডার দেখা ও স্ট্যাটাস আপডেট করা যাবে

---

## 👤 ধাপ ১০: গ্রাহকের জন্য "আমার অর্ডার" পেজ (`/orders`)

সাইটের সাথে **`/orders`** লিখলেই (যেমন `https://আপনার-সাইট.pages.dev/orders`) একটা আলাদা পেজ খুলবে, যেখানে গ্রাহক নিজে:

- 📦 **অর্ডার ট্র্যাক করতে পারবে** — শুধু অর্ডার আইডি + মোবাইল নম্বর দিয়ে (লগইন করা লাগে না), বর্তমান স্ট্যাটাস (অপেক্ষমান → নিশ্চিত → পাঠানো হয়েছে → ডেলিভার্ড) টাইমলাইন সহ দেখতে পারবে
- 👤 **অ্যাকাউন্ট তৈরি/লগইন করতে পারবে** — নাম, মোবাইল নম্বর ও একটা পাসওয়ার্ড দিয়ে (ইমেইল ঐচ্ছিক)
- 📋 **অর্ডার হিস্টোরি দেখতে পারবে** — লগইন করা থাকলে তার নামে করা সব অর্ডার (আইডি, তারিখ, মূল্য, স্ট্যাটাস সহ) এক জায়গায়

হোমপেজের **📦 অর্ডার ট্র্যাক** ও **👤 লগইন/অ্যাকাউন্ট** লিংকগুলো এই পেজেই নিয়ে যাবে। গ্রাহক লগইন করা অবস্থায় চেকআউট করলে সেই অর্ডারটা স্বয়ংক্রিয়ভাবে তার অ্যাকাউন্টের সাথে যুক্ত হয়ে যাবে (গেস্ট হিসেবে অর্ডার করলেও অর্ডার আইডি + মোবাইল নম্বর দিয়ে ট্র্যাক করা যাবে, অ্যাকাউন্ট বাধ্যতামূলক না)।

এই ফিচারটার জন্য আলাদা কোনো সেটআপ লাগে না — ধাপ ৫-এ `schema.sql` চালালেই গ্রাহক-লগইনের টেবিলগুলো (customers, customer_sessions) তৈরি হয়ে যায়।

> 🔄 **আগে থেকেই সাইট চালু আছে?** যদি আপনি এই আপডেটের **আগে** একবার `schema.sql` চালিয়ে সাইট ডেপ্লয় করে ফেলে থাকেন, তাহলে নতুন করে পুরো schema.sql না চালিয়ে শুধু মাইগ্রেশন ফাইলটা চালান (এতে পুরনো পণ্য/অর্ডার ডেটা অক্ষত থাকবে):
> ```bash
> npx wrangler d1 execute nextgentechbd-db --remote --file=./migrations/001_add_customer_accounts.sql
> ```
> নতুন করে সাইট বসাচ্ছেন? এই ধাপ লাগবে না — ধাপ ৫-এর `schema.sql`-এই সবকিছু আগে থেকে আছে।
>
> 🔄 **হিরো ব্যানার/প্রোমো কার্ড এডিট করার ফিচার নতুন যোগ হয়েছে?** আগে থেকেই সাইট চালু থাকলে এই মাইগ্রেশনটাও চালাতে হবে (নাহলে অ্যাডমিন প্যানেলে হোমপেজ টেক্সট ট্যাবে এরর আসবে):
> ```bash
> npx wrangler d1 execute nextgentechbd-db --remote --file=./migrations/004_add_hero_promo_text.sql
> ```

---

## 🔑 ধাপ ১১: পাসওয়ার্ড ভুলে গেলে (Forgot Password)

এটা সম্পূর্ণ **স্বয়ংক্রিয় ও তাৎক্ষণিক** — কোনো অ্যাডমিন সহায়তা বা হোয়াটসঅ্যাপ লাগে না:

1. `/orders` পেজে লগইন ফর্মের নিচে **"পাসওয়ার্ড ভুলে গেছেন?"** চাপবে
2. তার মোবাইল নম্বর দেবে
3. নম্বরটার সাথে অ্যাকাউন্ট থাকলে সাথে সাথেই **"নতুন পাসওয়ার্ড সেট করুন"** ধাপে চলে যাবে
4. নতুন পাসওয়ার্ড দিয়ে সাবমিট করলেই পাসওয়ার্ড বদলে যাবে — এরপর নতুন পাসওয়ার্ড দিয়ে লগইন করতে পারবে

⚠️ **নিরাপত্তা নোট:** এই ফ্লো-তে শুধু মোবাইল নম্বর জানলেই যে কেউ পাসওয়ার্ড বদলে দিতে পারবে (OTP/ইমেইল/হোয়াটসঅ্যাপ যাচাই নেই) — এটা ইচ্ছাকৃতভাবে সরল রাখা হয়েছে যাতে কোনো থার্ড-পার্টি ইমেইল/এসএমএস সার্ভিস সেটআপ করা না লাগে। আপনার গ্রাহকদের মোবাইল নম্বর যথেষ্ট গোপনীয় বলে মনে করলে ভবিষ্যতে চাইলে এখানে একটা যাচাইকরণ ধাপ (OTP ইত্যাদি) যোগ করা যাবে।

এই ফিচারটার জন্য আলাদা কোনো সেটআপ লাগে না — ধাপ ৫-এ `schema.sql` চালালেই গ্রাহক-লগইনের টেবিলগুলো তৈরি হয়ে যায়।

---

## 🖼️ ধাপ ১২: পণ্যের ছবি আপলোডের জন্য GitHub টোকেন সেটআপ (৫ মিনিট)

অ্যাডমিন প্যানেল থেকে পণ্যের আসল ছবি আপলোড করলে সেটা সরাসরি আপনার **GitHub রিপোজিটরিতে কমিট** হয়ে যায় (`public/uploads/products/` ফোল্ডারে) — তাই **কোনো কার্ড/বিলিং সেটআপ লাগে না**, সম্পূর্ণ ফ্রি।

**কীভাবে কাজ করে:** ছবি আপলোড করলে সেটা GitHub-এ কমিট হয় → Cloudflare Pages স্বয়ংক্রিয়ভাবে নতুন ডিপ্লয় শুরু করে (কারণ প্রজেক্টটা GitHub-এর সাথে যুক্ত) → **১-২ মিনিট পর** ছবিটা লাইভ সাইটে দেখা যায়। মানে ছবি সাথে সাথে দেখা যাবে না, একটু অপেক্ষা করতে হবে — কিন্তু বিনিময়ে কোনো এক্সট্রা সার্ভিস/কার্ড লাগে না।

### GitHub Personal Access Token তৈরি করুন

1. **github.com** এ লগইন করুন → উপরে ডান পাশে প্রোফাইল ছবি → **Settings**
2. একদম নিচে বাম মেনুতে **Developer settings** → **Personal access tokens** → **Fine-grained tokens**
3. **Generate new token** চাপুন
4. পূরণ করুন:
   - Token name: `nextgentechbd-image-upload`
   - Expiration: সুবিধামতো (যেমন 1 year)
   - Repository access: **Only select repositories** → আপনার `nextgentechbd` রিপোটা বেছে নিন
   - Permissions → **Repository permissions** এ নিচে স্ক্রল করে **Contents** খুঁজুন → **Read and write** সিলেক্ট করুন
5. **Generate token** চাপুন
6. যে টোকেনটা দেখাবে (`github_pat_...` দিয়ে শুরু) সেটা **এখনই কপি করে কোথাও সেভ করে রাখুন** — এই পেজ থেকে সরে গেলে আর দেখা যাবে না

### Cloudflare Pages-এ Environment Variables যোগ করুন

1. আপনার **nextgentechbd** Pages প্রজেক্টে যান → **Settings** → **Environment variables**
2. **Production** এর নিচে **Add variable** চেপে একে একে এই ৩টা যোগ করুন:

   | Variable name | Value | Encrypt? |
   |---|---|---|
   | `GITHUB_TOKEN` | কপি করা টোকেনটা পেস্ট করুন | ✅ হ্যাঁ (🔒 চালু রাখুন) |
   | `GITHUB_OWNER` | আপনার GitHub ইউজারনেম | না লাগবে না |
   | `GITHUB_REPO` | রিপোজিটরির নাম (যেমন `nextgentechbd`) | না লাগবে না |

3. **Save** করুন

### রিডেপ্লয় করুন

1. **Deployments** ট্যাবে যান → সর্বশেষ ডেপ্লয়মেন্টের পাশে **⋯** → **Retry deployment**
2. ১-২ মিনিট অপেক্ষা করুন

### টেস্ট করুন

- `/admin/` → পণ্যসমূহ → নতুন/এডিট পণ্য ফর্মে **"পণ্যের ছবি"** থেকে একটা ছবি (সর্বোচ্চ ২MB, JPG/PNG/WEBP/GIF) সিলেক্ট করুন
- সাথে সাথে একটা প্রিভিউ দেখাবে এবং "GitHub-এ কমিট হয়েছে" মেসেজ আসবে — এটা মানে আপলোড সফল হয়েছে
- পণ্যটা **Save** করে ১-২ মিনিট পর সাইট রিফ্রেশ করলে ছবিটা লাইভ দেখা যাবে

> 💡 GitHub টোকেন সেটআপ না করলেও সাইট স্বাভাবিকভাবে চলবে (পণ্যের ইমোজি আইকন আগের মতোই দেখাবে) — শুধু "ছবি আপলোড" অংশটা কাজ করবে না যতক্ষণ না এই ধাপ সম্পন্ন হয়।
>
> ⚠️ **মনে রাখবেন:** প্রতিটা ছবি আপলোডে একটা নতুন GitHub কমিট ও Cloudflare Pages ডিপ্লয় হয়। Cloudflare Pages ফ্রি টিয়ারে মাসে ৫০০টা ডিপ্লয়মেন্টের লিমিট আছে — স্বাভাবিক ব্যবহারে (মাসে কয়েক ডজন পণ্য/ছবি) এটা কোনো সমস্যা হবে না।

---

## 📝 ধাপ ১৩: হোমপেজের সেকশন-শিরোনাম বদলানো

`/admin/` → **📝 হোমপেজ টেক্সট** ট্যাবে গিয়ে নিচের ৩টা শিরোনাম যেকোনো সময় বদলাতে পারবেন:
- ⚡ ফ্ল্যাশ সেল সেকশনের শিরোনাম
- 🗂️ ক্যাটাগরি সেকশনের শিরোনাম
- 🎯 "আপনার জন্য বাছাই করা" সেকশনের শিরোনাম

Save করলেই সরাসরি সাইটে (রিফ্রেশ করলে) দেখা যাবে — কোনো কোড পরিবর্তন বা রিডেপ্লয় লাগে না।

---

## 📋 রোজকার ব্যবহারের নিয়ম

| কাজ | কীভাবে |
|---|---|
| নতুন অর্ডার দেখা | হোয়াটসঅ্যাপ মেসেজ (প্রধান) + `/admin/` → অর্ডারসমূহ (ব্যাকআপ) |
| পণ্য যোগ/বদল/মুছা/ছবি আপলোড | `/admin/` → পণ্যসমূহ |
| ক্যাটাগরি যোগ/বদল/মুছা | `/admin/` → ক্যাটাগরি |
| যোগাযোগ/পেমেন্ট নম্বর বদল | `/admin/` → দোকানের সেটিংস |
| হোমপেজের সেকশন-শিরোনাম বদল | `/admin/` → হোমপেজ টেক্সট |
| ফুটারের নীতিমালা (About/Terms/Delivery/Return) বদল | `public/index.html` ফাইলে `INFO_CONTENT` অবজেক্ট এডিট করে GitHub-এ আপডেট দিন (রিডেপ্লয় হয়ে যাবে) |
| অ্যাডমিন কী বদলানো/হারিয়ে ফেললে | Pages → Settings → Environment variables → `ADMIN_KEY` এডিট করে নতুন কী দিন → রিডেপ্লয় করুন |
| গ্রাহক নিজের অর্ডার ট্র্যাক/হিস্টোরি দেখতে | `/orders` → অর্ডার আইডি+নম্বর দিয়ে ট্র্যাক অথবা লগইন করে হিস্টোরি |

---

## 🔧 সমস্যা হলে (Troubleshooting)

| সমস্যা | সমাধান |
|---|---|
| সাইটে পণ্য দেখাচ্ছে না / "সার্ভারে সমস্যা" লেখা আসছে | D1 বাইন্ডিং (`DB` নামে) ঠিকমতো যোগ করা আছে কি না দেখুন (ধাপ ৪), schema.sql ও seed.sql রান হয়েছে কি না দেখুন (ধাপ ৫), তারপর রিডেপ্লয় করুন |
| `/admin/` এ লগইন হচ্ছে না | `ADMIN_KEY` Environment variable ঠিকমতো সেট করা আছে কি না দেখুন (ধাপ ৬), এবং সেট করার পর রিডেপ্লয় করেছেন কি না দেখুন |
| "ADMIN_KEY সেট করা হয়নি" এরর | ধাপ ৬ আবার করুন এবং **Production** এনভায়রনমেন্টে সেট করেছেন তা নিশ্চিত করুন |
| `wrangler d1 execute` কমান্ড এরর দেখাচ্ছে | `npx wrangler login` দিয়ে লগইন করা আছে কি না দেখুন, এবং ডেটাবেজের নাম `nextgentechbd-db` ঠিক আছে কি না মিলিয়ে দেখুন |
| হোয়াটসঅ্যাপ লিংক কাজ করছে না | `/admin/` → দোকানের সেটিংস → হোয়াটসঅ্যাপ নম্বর সঠিক ফরম্যাটে আছে কি না দেখুন (দেশের কোডসহ, + ছাড়া — যেমন `8801717877955`) |
| `/orders` পেজে ট্র্যাক/লগইন কাজ করছে না | D1 বাইন্ডিং ও schema.sql ঠিকমতো চালানো আছে কি না দেখুন (ধাপ ৪-৫); আগে থেকে সাইট চালু থাকলে migration ফাইলটা চালিয়েছেন কি না দেখুন (ধাপ ১০) |
| "এই তথ্য দিয়ে কোনো অর্ডার পাওয়া যায়নি" | অর্ডার আইডি ও যে মোবাইল নম্বর দিয়ে অর্ডার করা হয়েছিল ঠিক সেটাই দেওয়া হয়েছে কি না মিলিয়ে দেখুন — দুটোই মিলতে হবে |
| "পাসওয়ার্ড ভুলে গেছেন" এ "এই নম্বরে কোনো অ্যাকাউন্ট পাওয়া যায়নি" | গ্রাহক যে নম্বর দিয়ে অ্যাকাউন্ট রেজিস্ট্রেশন করেছিল ঠিক সেই নম্বরটাই দিচ্ছে কি না দেখুন |
| পণ্যের ছবি আপলোড করলে এরর আসছে ("GitHub আপলোড কনফিগার করা নেই") | ধাপ ১২ অনুসরণ করে GitHub টোকেন বানিয়ে GITHUB_TOKEN/GITHUB_OWNER/GITHUB_REPO যোগ করুন, তারপর রিডেপ্লয় করুন |
| আপলোড করা ছবি ১-২ মিনিট পরও সাইটে দেখাচ্ছে না | Cloudflare Pages-এর **Deployments** ট্যাবে চেক করুন নতুন ডিপ্লয় শুরু/শেষ হয়েছে কি না; GITHUB_TOKEN-এর মেয়াদ/অনুমতি ঠিক আছে কি না দেখুন |
| ছবি আপলোডে "HTTP 401" এরর | GITHUB_TOKEN ভুল বসানো হয়েছে বা মেয়াদ শেষ হয়ে গেছে — নতুন টোকেন বানিয়ে আবার বসান |
| ছবি আপলোডে "HTTP 404" এরর | GITHUB_OWNER/GITHUB_REPO বানান ভুল আছে কি না দেখুন, অথবা টোকেনে এই রিপোর Contents পারমিশন ঠিকমতো দেওয়া হয়নি |
| হোমপেজ টেক্সট সেভ করলেও পুরনো লেখা দেখাচ্ছে | ব্রাউজারে হার্ড রিফ্রেশ (Ctrl+Shift+R / Cmd+Shift+R) দিয়ে দেখুন, ক্যাশের কারণে পুরনো ভার্সন দেখাতে পারে |

---

## 🌟 পরে করা যাবে (ঐচ্ছিক)

- **নিজের ডোমেইন**: Pages প্রজেক্ট → **Custom domains** → **Set up a custom domain**
- **অনলাইন পেমেন্ট গেটওয়ে**: SSLCommerz/bKash Merchant API যোগ করতে চাইলে `functions/api/` এ নতুন ফাংশন যোগ করে করা যাবে
- **অতিরিক্ত নিরাপত্তা**: চাইলে Cloudflare Access দিয়ে `/admin/` পেজে আরেক স্তরের লগইন যোগ করা যায়

**শুভকামনা! আপনার ব্যবসার সাফল্য কামনা করি 🧡**
