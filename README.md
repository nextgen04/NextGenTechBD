# 🛍️ NextGenTechBD — Cloudflare Pages + D1 ব্যাকএন্ড সহ ই-কমার্স সাইট

ডারাজ-স্টাইল ই-কমার্স ফ্রন্টএন্ড, যা **Cloudflare Pages**-এ হোস্ট হয় এবং **Cloudflare Pages Functions + D1 SQL ডেটাবেজ** দিয়ে বানানো নিজস্ব ব্যাকএন্ড API ব্যবহার করে। পণ্য, ক্যাটাগরি, সেটিংস ও অর্ডার — সবকিছু D1 ডেটাবেজে সংরক্ষিত থাকে, এবং একটা কাস্টম অ্যাডমিন প্যানেল থেকে ম্যানেজ করা যায়।

> ## 🚀 পাবলিশ করতে চান?
> **[DEPLOY-GUIDE.md](DEPLOY-GUIDE.md)** ফাইলটি দেখুন — GitHub + Cloudflare Pages + D1 দিয়ে ধাপে ধাপে বাংলা গাইড।

## 📁 ফাইল কাঠামো

```
nextgentechbd/
├── wrangler.toml            ← Cloudflare Pages/D1/R2 কনফিগ
├── package.json              ← সুবিধার জন্য wrangler স্ক্রিপ্ট (db:init, db:seed, db:migrate:customers, dev, deploy)
├── schema.sql                 ← D1 ডেটাবেজের টেবিল কাঠামো (পণ্য/ক্যাটাগরি/সেটিংস/অর্ডার + গ্রাহক লগইন + হোমপেজ টেক্সট)
├── seed.sql                   ← পুরনো পণ্য/ক্যাটাগরি/সেটিংস ডেটা (প্রথমবার লোড করার জন্য)
├── migrations/
│   ├── 001_add_customer_accounts.sql  ← আগে থেকে চালু থাকা সাইটে গ্রাহক লগইন যোগ করার মাইগ্রেশন
│   └── 003_add_site_text.sql          ← আগে থেকে চালু থাকা সাইটে হোমপেজ-টেক্সট এডিটিং যোগ করার মাইগ্রেশন
├── original-data-backup/      ← আগের products.json/settings.json ফাইল (শুধু রেফারেন্সের জন্য, এখন অ্যাপ এগুলো ব্যবহার করে না)
├── functions/
│   ├── _lib/utils.js         ← কমন হেল্পার (JSON রেসপন্স, অ্যাডমিন-কী যাচাই, পাসওয়ার্ড হ্যাশিং, সেশন টোকেন)
│   ├── images/
│   │   └── [[path]].js       ← GET /images/* (পাবলিক — R2 বাকেটে সংরক্ষিত পণ্যের ছবি সার্ভ করে)
│   └── api/
│       ├── products.js       ← GET /api/products (পাবলিক)
│       ├── settings.js       ← GET /api/settings (পাবলিক)
│       ├── site-text.js      ← GET /api/site-text (পাবলিক — হোমপেজের সম্পাদনাযোগ্য সেকশন-শিরোনাম)
│       ├── orders.js         ← POST /api/orders (পাবলিক — চেকআউট থেকে অর্ডার সেভ হয়, লগইন থাকলে অ্যাকাউন্টের সাথে যুক্ত হয়)
│       ├── orders/
│       │   └── track.js      ← GET /api/orders/track (পাবলিক — অর্ডার আইডি + মোবাইল নম্বর দিয়ে ট্র্যাকিং)
│       ├── customer/
│       │   ├── register.js       ← POST /api/customer/register (নতুন গ্রাহক অ্যাকাউন্ট)
│       │   ├── login.js          ← POST /api/customer/login
│       │   ├── logout.js         ← POST /api/customer/logout
│       │   ├── me.js             ← GET /api/customer/me (সেশন টোকেন দিয়ে যাচাই)
│       │   ├── orders.js         ← GET /api/customer/orders (লগইন করা গ্রাহকের অর্ডার হিস্টোরি)
│       │   ├── forgot-password.js ← POST /api/customer/forgot-password (মোবাইল নম্বর যাচাই)
│       │   └── reset-password.js  ← POST /api/customer/reset-password (নম্বর + নতুন পাসওয়ার্ড দিয়ে সরাসরি সেট)
│       └── admin/
│           ├── login.js      ← POST /api/admin/login
│           ├── products.js   ← GET/POST/DELETE — পণ্য CRUD (X-Admin-Key লাগবে)
│           ├── upload.js     ← POST /api/admin/upload — পণ্যের ছবি R2-তে আপলোড (X-Admin-Key লাগবে)
│           ├── categories.js ← GET/POST/DELETE — ক্যাটাগরি CRUD (X-Admin-Key লাগবে)
│           ├── settings.js   ← GET/PUT — সেটিংস (X-Admin-Key লাগবে)
│           ├── site-text.js  ← GET/PUT — হোমপেজের সেকশন-শিরোনাম (X-Admin-Key লাগবে)
│           └── orders.js     ← GET/PATCH — অর্ডার তালিকা ও স্ট্যাটাস (X-Admin-Key লাগবে)
└── public/
    ├── index.html             ← দোকানের ফ্রন্টএন্ড (SPA), ডেটা লোড হয় /api/* থেকে
    ├── uploads/
    ├── admin/
    │   └── index.html         ← কাস্টম অ্যাডমিন প্যানেল (ভ্যানিলা JS, নিজস্ব API কল করে)
    └── orders/
        └── index.html         ← গ্রাহকের "আমার অর্ডার" পেজ — /orders এ পাওয়া যায় (ট্র্যাকিং + লগইন/রেজিস্টার + অর্ডার হিস্টোরি)
```

## 🔐 অ্যাডমিন প্যানেল কীভাবে কাজ করে

অ্যাডমিন প্যানেল একটা সিক্রেট **ADMIN_KEY** দিয়ে সুরক্ষিত, যা Cloudflare Pages-এর Environment Variable হিসেবে সেট করা হয়। লগইন করার পর ব্রাউজার প্রতিটি অ্যাডমিন রিকোয়েস্টে এই কী পাঠায় (`X-Admin-Key` হেডারে), এবং ব্যাকএন্ড ফাংশন সেটা যাচাই করে তবেই ডেটাবেজে পরিবর্তন করতে দেয়।

- URL: `https://আপনার-সাইট.pages.dev/admin/`
- পণ্য/ক্যাটাগরি/সেটিংস/অর্ডার — সব পরিবর্তন সরাসরি D1 ডেটাবেজে সেভ হয়, সাইট রিফ্রেশ করলেই দেখা যায় (কোনো রিবিল্ড লাগে না)

বিস্তারিত সেটআপ ধাপ **DEPLOY-GUIDE.md** তে আছে।

## 👤 গ্রাহকের "আমার অর্ডার" পেজ (`/orders`)

সাইটে `/orders` (যেমন `https://আপনার-সাইট.pages.dev/orders`) খুললে গ্রাহক নিজে:

- **অর্ডার ট্র্যাক** করতে পারবে — শুধু অর্ডার আইডি (চেকআউটের পর তৈরি হওয়া, যেমন `SB12345678`) + মোবাইল নম্বর দিয়ে, লগইন ছাড়াই
- **অ্যাকাউন্ট তৈরি/লগইন** করতে পারবে — নাম, মোবাইল নম্বর ও পাসওয়ার্ড দিয়ে
- **অর্ডার হিস্টোরি** দেখতে পারবে — লগইন করা থাকলে তার নামে করা সব অর্ডার আইডি, স্ট্যাটাস, মূল্য সহ

হোমপেজের **📦 অর্ডার ট্র্যাক** ও **👤 লগইন/অ্যাকাউন্ট** লিংক এখন এই পেজে নিয়ে যায়। গ্রাহক লগইন করা অবস্থায় চেকআউট করলে অর্ডারটা স্বয়ংক্রিয়ভাবে তার অ্যাকাউন্টে যুক্ত হয়ে যায়; লগইন ছাড়াও (গেস্ট চেকআউট) অর্ডার আইডি + মোবাইল নম্বর দিয়ে ট্র্যাক করা যায়।

**পাসওয়ার্ড ভুলে গেলে:** গ্রাহক `/orders` পেজে "পাসওয়ার্ড ভুলে গেছেন?" চেপে তার মোবাইল নম্বর দিলেই — নম্বরটার সাথে অ্যাকাউন্ট থাকলে সরাসরি "নতুন পাসওয়ার্ড সেট করুন" ধাপে চলে যায়, কোনো অ্যাডমিন সহায়তা বা ইমেইল/এসএমএস/হোয়াটসঅ্যাপ যাচাই লাগে না।

## ✨ দোকানের ফিচার

| ফিচার | বিবরণ |
|---|---|
| 🗂️ ক্যাটাগরি | ৮টি ক্যাটাগরি — ইলেকট্রনিক্স, ফ্যাশন, হোম, বিউটি, গ্রোসারি, খেলনা, স্পোর্টস, বই |
| ⚡ ফ্ল্যাশ সেল | লাইভ কাউন্টডাউন + স্টক প্রগ্রেস বার |
| 🔎 সার্চ ও সর্টিং | বাংলা সার্চ, ৫ ধরনের সর্টিং |
| 🛒 কার্ট | স্লাইডিং ড্রয়ার, ফ্রি ডেলিভারি প্রগ্রেস (৳১৯৯৯+) |
| 🚚 চেকআউট | ভ্যালিডেশন সহ ফর্ম — COD / বিকাশ / নগদ / রকেট |
| 💬 অর্ডার সাবমিশন | চেকআউট করলেই অর্ডারের বিস্তারিত হোয়াটসঅ্যাপে চলে যায় + D1 ডেটাবেজে ব্যাকআপ থাকে |
| ❤️ উইশলিস্ট | ব্রাউজারে সংরক্ষিত |
| 📱 রেসপন্সিভ | মোবাইল বটম-নেভিগেশন সহ |
| 🗄️ D1 ব্যাকএন্ড | পণ্য/ক্যাটাগরি/সেটিংস/অর্ডার — সব একটা আসল SQL ডেটাবেজে সংরক্ষিত |
| 👤 গ্রাহক অ্যাকাউন্ট | রিয়েল লগইন/রেজিস্ট্রেশন (পাসওয়ার্ড হ্যাশ করে সংরক্ষিত), অর্ডার হিস্টোরি |
| 🔑 পাসওয়ার্ড রিসেট | মোবাইল নম্বর যাচাই করে সরাসরি নতুন পাসওয়ার্ড সেট (তাৎক্ষণিক, কোনো OTP/ইমেইল লাগে না) |
| 📦 অর্ডার ট্র্যাকিং | অর্ডার আইডি + মোবাইল নম্বর দিয়ে যেকোনো অর্ডারের বর্তমান অবস্থা ট্র্যাক (`/orders`) |
| 🖼️ পণ্যের ছবি আপলোড | অ্যাডমিন প্যানেল থেকে সরাসরি ছবি আপলোড (Cloudflare R2-তে সংরক্ষিত, ইমোজির বদলে আসল ছবি দেখায়) |
| 📄 ফুটার নীতিমালা পপআপ | About/Terms/Delivery/Return — নতুন ট্যাব না খুলে স্ক্রিনেই পপআপ আকারে দেখায় |
| 📝 হোমপেজ টেক্সট এডিটিং | ফ্ল্যাশ সেল/ক্যাটাগরি/আপনার জন্য — সেকশনের শিরোনাম অ্যাডমিন প্যানেল থেকে বদলানো যায় |

## 📞 যোগাযোগ ও পেমেন্ট (ডিফল্ট)

- ফোন / হোয়াটসঅ্যাপ / টেলিগ্রাম: **০১৭১৭৮৭৭৯৫৫**
- ফেসবুক: https://www.facebook.com/share/19KVN4rZkS/
- ইমেইল: tnextgen04@gmail.com
- পেমেন্ট: বিকাশ, নগদ, রকেট (০১৭১৭৮৭৭৯৫৫) + ক্যাশ অন ডেলিভারি

এই তথ্যগুলো D1 ডেটাবেজের `settings` টেবিলে থাকে এবং অ্যাডমিন প্যানেলের **⚙️ দোকানের সেটিংস** অংশ থেকে যেকোনো সময় বদলানো যাবে।

## 🚀 লোকালি প্রিভিউ করার নিয়ম

```bash
npm install
npx wrangler d1 execute nextgentechbd-db --local --file=./schema.sql
npx wrangler d1 execute nextgentechbd-db --local --file=./seed.sql
npm run dev
```

তারপর টার্মিনালে দেখানো লোকাল URL (সাধারণত `http://localhost:8788`) ব্রাউজারে খুলুন। লোকাল মোডে D1-এর একটা লোকাল কপি ব্যবহার হয় (আসল ডেটাবেজ প্রভাবিত হয় না), এবং `ADMIN_KEY` টেস্ট করতে `.dev.vars` ফাইলে `ADMIN_KEY=test-key-123` লিখে রাখতে পারেন।

## ⚠️ নোট

- অর্ডার পেমেন্ট এখনো ম্যানুয়াল (বিকাশ/নগদ/রকেট সেন্ড মানি + COD) — সরাসরি পেমেন্ট গেটওয়ে (SSLCommerz/bKash Merchant) নেই
- Cloudflare Pages/D1-এর ফ্রি টিয়ারেই এই সাইট চালানো যাবে; ট্রাফিক অনেক বেড়ে গেলে Cloudflare-এর প্ল্যান দেখে নিন
- ADMIN_KEY-কে পাসওয়ার্ডের মতোই গোপন রাখুন — এটাই এখন অ্যাডমিন প্যানেলের একমাত্র সুরক্ষা
